"use client";

/**
 * Static asset paths.
 *
 * App screenshots live in /public/Projects. Drop your own PNG/JPG in there,
 * add a `const myApp = "/Projects/my-app.png";` line below, expose it in the
 * `images` object, then reference it as `images.myApp` from data.js.
 */

const profile = "/profile.jpg";
const placeholder = "/Projects/placeholder.svg";
const daamLawyer = "/Projects/daam-lawyer.jpg";
const birdAds = "/Projects/bird-ads.jpg";

const images = {
  profile,
  placeholder,
  daamLawyer,
  birdAds,
};

export default images;
